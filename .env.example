# R.I.T.H.M Environment Variables
# Copy to .env and fill in values for production
# ⚠️  NEVER commit .env to git!

# Django settings (REQUIRED in production)
DEBUG=False
SECRET_KEY=generate-a-secure-random-key-here
ALLOWED_HOSTS=rithm.app,www.rithm.app

# Generate a secure SECRET_KEY with:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# Database (PostgreSQL) - REQUIRED in production
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgres://username:password@localhost:5432/rithm

# Note: For Heroku/Railway/Render, DATABASE_URL is usually auto-set
